interface:
  display_name: "Track Nutrition"
  short_description: "Automatic nutrition tracking with UI-only replies"
  default_prompt: "Use $track-nutrition, make reasonable assumptions, act immediately, and return only its UI unless one blocking question is unavoidable."
policy:
  allow_implicit_invocation: true
